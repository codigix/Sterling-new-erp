const db = require('./config/db'); const bcrypt = require('bcryptjs'); async function updatePasswords() { try { const salt = await bcrypt.genSalt(10); const hashedPassword = await bcrypt.hash('12345678', salt); const emails = ['admin@gmail.com', 'design@gmail.com', 'production@gmail.com', 'procurement@gmail.com', 'inventory@gmail.com', 'quality@gmail.com']; for (const email of emails) { await db.query('UPDATE users SET password = ? WHERE email = ?', [hashedPassword, email]); console.log('Updated password for:', email); } process.exit(0); } catch (error) { console.error('Error:', error.message); process.exit(1); } } updatePasswords();  
